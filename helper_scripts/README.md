# helper_scripts
segment_downsample.py generates points data in TSV format and images for debug.
ransac_python.py runs the algorithm on the TSV file and plots a graph to show results.
images folder contains example images.
tsv folder contains generated TSVs.